apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "trmnl.fullname" . }}-test-connection"
  labels:
    {{- include "trmnl.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['--spider', '{{ include "trmnl.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
